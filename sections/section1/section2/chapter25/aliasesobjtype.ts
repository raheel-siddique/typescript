// console.log("new 24");
// you can make custom type of data type

// how can we use custom types with object
 type objTypeAll={name:number, age:number}
const person:objTypeAll={
    name:"Raheel",
    age:12,
}
console.log(person.name);